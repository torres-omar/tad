div class="gauge-container d-flex justify-content-center mt-5"
    div class="gauge-outer-under gauge-outer"
    div class="gauge-inner"
    div class="gauge-outer-over gauge-outer" id="gauge-outer-over_year" data-ratio="#{offer_acceptance_ratio_data_for_year(current: true)[:ratio]}"
    div class="gauge-data" id="gauge-data_year" #{offer_acceptance_ratio_data_for_year(current: true)[:ratio]}
